import React, { Component } from 'react';

const Form = (getFunction) => {
    return (
        <div>
            <form onSubmit="" >
                <input type="text" name="field" placeholder="Enter a product name" />
                <button>Add</button>
            </form>
        </div>
    )
}

export default Form; 